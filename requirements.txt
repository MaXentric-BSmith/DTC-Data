# MaXentric DTC age-classifier submission
# Recommended: Python 3.11 or 3.12 (64-bit). See README.md for install notes.
#
# Full stack (tree models + notebooks + ResNet-1D CNN):
#   python -m venv .venv && source .venv/bin/activate
#   pip install -U pip
#   pip install -r requirements.txt
#
# Tree-model / visualization only (no PyTorch): see README.md — install packages above the torch lines manually.

# --- Core numerics (NumPy 1.x ABI matches current PyTorch wheels on macOS/Linux) ---
numpy>=1.26,<2
pandas>=2.1,<3
scipy>=1.11,<2

# --- Classical ML pipelines (heart_age_classifier.py, notebooks) ---
scikit-learn>=1.4,<2
joblib>=1.3,<2
xgboost>=2.0,<3

# --- Plotting & interactive notebooks ---
matplotlib>=3.8,<4
seaborn>=0.13,<1
ipywidgets>=8.1,<9
ipykernel>=6.29,<7

# --- Deep learning (create_age_classifier.ipynb ResNet-1D; optional on Python 3.13+) ---
torch>=2.2.0,<2.3; python_version >= "3.11" and python_version < "3.13"
torchvision>=0.17.0,<0.18; python_version >= "3.11" and python_version < "3.13"
torchaudio>=2.2.0,<2.3; python_version >= "3.11" and python_version < "3.13"
